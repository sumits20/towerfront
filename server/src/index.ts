// Plain Node/TypeScript server skeleton (build plan action #4). Intentionally
// does nothing yet — the current phase is the local combat sandbox in
// client/, which has no network dependency. This becomes the Colyseus match
// server once the local MVP is complete (build plan section 15, milestone
// "Online multiplayer alpha").
console.log("towerfront-server skeleton: nothing to run yet, see build plan section 15");
